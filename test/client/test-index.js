const GameContainer = require('../client/js/components/game_container');

require('chai').should();

describe('GameContainer', function() {
    it('Should get props from Redux and render the page.');
  }
);
